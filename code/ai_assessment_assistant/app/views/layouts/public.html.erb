<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "AI Assessment - LaunchPad Lab" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Include application stylesheet which should contain Tailwind %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    
    <%# Tailwind CSS via CDN as fallback for development %>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              'lpl-blue': '#1E60BD',
              'lpl-cyan': '#00ADEF'
            }
          }
        }
      }
    </script>
    
    <%= javascript_importmap_tags %>
  </head>

  <body class="antialiased">
    <%# Flash messages for public pages %>
    <% if notice %>
      <div class="fixed top-4 left-1/2 transform -translate-x-1/2 z-50 bg-green-500/90 backdrop-blur-sm text-white px-6 py-3 rounded-lg shadow-lg">
        <%= notice %>
      </div>
    <% end %>
    
    <% if alert %>
      <div class="fixed top-4 left-1/2 transform -translate-x-1/2 z-50 bg-red-500/90 backdrop-blur-sm text-white px-6 py-3 rounded-lg shadow-lg">
        <%= alert %>
      </div>
    <% end %>
    
    <%= yield %>
  </body>
</html> 